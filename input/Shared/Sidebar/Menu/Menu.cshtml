<nav class="menu">
    <ul class={styles['menu__list']}>
        {menu.map((item) => (
        <li class={styles['menu__list-item']} key={item.path}>
            <Link to={item.path}
                  class={styles['menu__list-item-link']}
                  activeclass={styles['menu__list-item-link--active']}>
            {item.label}
            </Link>
        </li>
        ))}
    </ul>
</nav>